
26.06.2023

CREATE TABLE vas_level_access (
    id             INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        level_access INTEGER,
        description_access VARCHAR(150)
)
ENGINE = InnoDB;

INSERT INTO vas_level_access (level_access, description_access)
VALUES (1, 'СУРВ, Добавить сотрудника, Провести проверку KIA');
INSERT INTO vas_level_access (level_access, description_access)
VALUES (2, 'СУРВ');
INSERT INTO vas_level_access (level_access, description_access)
VALUES (3, 'СУРВ');
INSERT INTO vas_level_access (level_access, description_access)
VALUES (4, 'СУРВ');
INSERT INTO vas_level_access (level_access, description_access)
VALUES (5, 'СУРВ');



CREATE TABLE vas_employers (
    id             INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    full_name VARCHAR(150) NOT NULL UNIQUE,
    subdivision VARCHAR(150) NOT NULL,
    job_title VARCHAR(250) NOT NULL,
    schedule VARCHAR(50) NOT NULL,
    bid INTEGER NOT NULL,
    code_level_access INTEGER NOT NULL,
    authorization_code VARCHAR(25) NOT NULL UNIQUE,
    key_id VARCHAR(50) UNIQUE,
    status VARCHAR(50) DEFAULT 'active'
)
ENGINE = InnoDB;

INSERT INTO vas_employers (full_name, subdivision, job_title, schedule, bid, code_level_access, authorization_code, key_id)
VALUES ('Шабанов Кирилл Владимирович', 'ОТОА', 'Заместитель директора по развитию сервиса', 'Пятидневный график', 1, 1, '001', 'Нет данных');
INSERT INTO vas_employers (full_name, subdivision, job_title, schedule, bid, code_level_access, authorization_code, key_id)
VALUES ('Комлев Илья Юрьевич', 'ОТОА', 'Инженер-технолог', 'Сменный график', 1, 3, '002', 'Нет');